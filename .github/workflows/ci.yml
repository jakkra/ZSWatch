on:
    pull_request:
    workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  build:
    uses: ./.github/workflows/build.yml

  test:
    needs: [build]
    uses: ./.github/workflows/test.yml
