on:
  pull_request_target:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  build:
    uses: ./.github/workflows/build.yml

  test:
    needs: [build]
    uses: ./.github/workflows/test.yml
